import { createStore } from 'vuex'
import auth from '@/store/modules/auth.js'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: { // sync
  },
  actions: {  // async
  },
  modules: {
    auth,
  }
})
